# =============================================
#  Jada — .gitignore
# =============================================

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
dist/
build/

# --- Entornos virtuales ---
.venv\
.venv
venv\
venv
env\
env
.env\
.env
# Note: we specifically ignore the directories to avoid tracking thousands of library files

# ─── SECRETOS — NUNCA en git ─────────────────
.env
.env.*
!.env.example     # sí subir el template sin valores reales

# ─── DATOS DE RUNTIME — se generan en el servidor ───────────────────────────
# Bases de datos locales (memoria, notas, etc.)
*.db
*.sqlite
*.sqlite3

# Estado del proceso
jada.pid
*.pid

# Logs
*.log
logs/

# Cronjobs: son datos de runtime, se crean/modifican mientras el bot corre.
# En producción deberían estar en MongoDB — ver ARCHITECTURE.md
cronjobs.json

# Caché del scheduler / matrix session
sync_token.json
token.json
client_store/
crypto_store/
session.json

# ─── ARCHIVOS QUE SÍ SUBEN A GIT ────────────────────────────────────────────
# .agent/*.md  — soul, user, heartbeat, identity — son configuración del agente
#               y se versiona junto con el código. ¡Estos SÍ van en git!
# Ejemplo: .agent/soul.md, .agent/user.md, .agent/heartbeat.md

# ─── IDE / OS ────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.code-workspace
.DS_Store
Thumbs.db
desktop.ini

# ─── Testing ────────────────────────────────────────────────────────────────
playwright-report/
test-results/

# ─── Archivos temporales ─────────────────────────────────────────────────────
*.tmp
_sample.json
tmp/

# ─── Node (dashboard) ────────────────────────────────────────────────────────
node_modules/
.next/
.env.local

credentials.json