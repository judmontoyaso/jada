# =============================================
#  MiniClaw — Configuración (EJEMPLO)
# =============================================
# ⚠️  COPIA ESTE ARCHIVO COMO .env Y COMPLETA CON TUS VALORES REALES
# ⚠️  NUNCA COMMITTEAR .env CON CLAVES REALES AL REPO
# =============================================

# --- LLM (NVIDIA NIM, OpenAI-compatible) ---
NVIDIA_API_KEY=nv-api-OBTENER_DE https://build.nvidia.com/minimaxai/minimax-m2.1
NVIDIA_MODEL=minimaxai/minimax-m2.1
NVIDIA_BASE_URL=https://integrate.api.nvidia.com/v1

# --- Matrix ---
MATRIX_HOMESERVER=https://tu-servidor-matrix.com
MATRIX_USER=@miniclaw:tu-servidor-matrix.com
MATRIX_PASSWORD=tu-password-aqui
MATRIX_ACCESS_TOKEN=  # Opcional: token de acceso directo (más seguro que password)
# IDs de rooms donde el bot escuchará (separados por comma)
MATRIX_ROOM_IDS=

# --- Agente ---
AGENT_NAME=MiniClaw
MAX_TOOL_ITERATIONS=10

# --- Memoria (SQLite) ---
MEMORY_DB_PATH=memory.db
MAX_HISTORY_CHARS=8000

# --- MongoDB (Gym Tracker) ---
MONGO_URI=mongodb://[tu-uri-de-conexion-aqui]
MONGO_DB=n8n_memoria
MONGO_GYM_COLLECTION=gimnasio

# --- Gmail (IMAP + SMTP) ---
# ⚠️ USAR APP PASSWORD, NO CONTRASEÑA PRINCIPAL
# Obtener App Password: https://myaccount.google.com/apppasswords
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993
IMAP_USER=tu-email@gmail.com
IMAP_PASSWORD=xxxx-xxxx-xxxx-xxxx

# --- Google Calendar (solo lectura) ---
# Obtener URL: Google Calendar > Settings > [Tu calendario] > "Secret address in iCal format"
GOOGLE_CALENDAR_ICS_URL=

# --- Browser ---
BROWSER_HEADLESS=true

# --- Seguridad Shell ---
# Comandos bloqueados (separados por comma)
BLOCKED_COMMANDS=rm -rf,format,del /f,shutdown,mkfs,chmod 777,chown

# --- Rate Limiting ---
RATE_LIMIT_PER_MINUTE=10  # Reducido de 15 a 10
RATE_LIMIT_BURST=3  # Máximo mensajes seguidos antes de cooldown

# --- LLM Timeout ---
LLM_TIMEOUT=120
MAX_MSG_LENGTH=2000

# --- Seguridad Adicional ---
# IPs o usuarios bloqueados (separados por comma)
BLOCKED_USERS=
# Timeout de sesión inactivity (segundos)
SESSION_TIMEOUT=3600
# Longitud máxima de entrada
MAX_INPUT_LENGTH=10000
